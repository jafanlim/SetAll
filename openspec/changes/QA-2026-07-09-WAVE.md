# QA Wave 2026-07-09 — Change Index (controller tracking sheet)

Source: user device QA 2026-07-09 (3 bugs + 5 dropped features). All items code-verified on
develop `f4d125d` by the analytical engineer 2026-07-10; root causes with file:line evidence in
each proposal. Each change = one workhorse dispatch cycle: branch off `develop`, PR back into
`develop`, controller verifies against the spec scenarios and ticks `tasks.md` in the PR.

Dispatch order (dependencies + size):

| # | Change | Pri | Why this order |
|---|---|---|---|
| 1 | `setall-region-date-format` | P1 | Smallest; unblocks clean verification of #2 (date display) |
| 2 | `setall-expense-date-edit` | P1 | Root cause pinned; has an open mirror-policy decision for controller |
| 3 | `setall-receipt-ocr-fallback-guard` | P1 | Investigation-first (Netlify logs + Vision quota) THEN code; includes OCR-failure auto-telemetry via the bug_reports pipeline |
| 4 | `setall-group-list-tile-parity` | P2 | Date + robust ≈ estimate on group list tiles |
| 5 | `setall-group-member-net-balances` | P2 | Extends PR #30 from "vs you" to true per-member nets |
| 6 | `setall-group-insights-entry` | P2 | Wiring only — analytics already group-filters |
| 7 | `setall-receipt-autoassign-disclaimer` | P3 | Trivial additive UI + i18n |

Hard gates on every PR (non-negotiable): money = `Decimal`/integer-cents (no float); RLS on any
new table (none expected in this wave); no service_role / hardcoded secrets; surgical diffs (no
whole-file reflow, no broad `git add`); `flutter analyze` = 0 AND full test suite green; a
"clean" merge that drops a feature's purpose = bounce (verify, don't trust). TDD
investigation-first: guard tests written red before fixes where a proposal says so. Tag genuine
workhorse LOGIC failures with `Eval-Task:` / `Eval-DeepSeek: failed` trailers.

Status legend: keep this table + each change's `tasks.md` current in every PR; fold ledger
updates into the same PR branch (develop only moves via merges).

## Progress log

- **Specs landed** — PR #54 (this tracker + 7 changes, docs only).
- **Pre-wave rescue** — PR #55: orphaned wallet-statement importer entry-point wired into the
  wallet "+" flow (ledger Task 6a, done 2026-06-29 but never committed).
- **#1 `setall-region-date-format`** — ✅ merged (PR #56). iOS/Android now read the system Region
  via `com.setall.app/region`. Controller correction on review (`6d87e73`): fixed a latent
  `patternFromLocale` bug the change newly exposed — YMD-country codes (`ja_JP`/`zh_CN`/`ko_KR`/
  `hu_HU`) resolved to DMY because the generic country→DMY branch preceded the YMD check;
  DeepSeek had deleted the failing `ja_JP` test instead of flagging it (`Eval-DeepSeek: failed`).
  482/482, analyze 0. **On-device (user 2026-07-10):** date ✅ (`en_GE`, DD/MM). **Time ✗** — device
  is 24h, app shows 12h. Fixed in `fix/region-time-format` (PR #59): native 24h detection appends
  `@hours=h23`/`h12` to the identifier; the real blocker was a `MissingPluginException` — the iOS
  channel wasn't registered under the new `FlutterImplicitEngineDelegate` embedding (registration
  moved to `didInitializeImplicitFlutterEngine` via the plugin registrar's messenger). **✅ On-device
  verified 2026-07-10: date (en_GE, DD/MM) AND time (24h) both correct — region-format bug closed.**
- **#2 `setall-expense-date-edit`** — ✅ merged (PR #58). Edit-expense date picker was dead UI:
  `_entryDate` never reached `updateExpense`, which also stripped `created_at`. Fix: optional
  `entryDate` → `created_at` in web + native update payloads (byte-identical strip when null, holding
  the PR #34 regression class); mirror `created_at` follows the source only when the date was
  explicitly edited (controller decision), else preserved. Clean DeepSeek delivery, no logic misses.
  4 hermetic tests, 486/486, analyze 0. **On-device verify (user) pending:** change the "07/06"
  expense's date → confirm the new date shows and the list reorders.
- **#3 `setall-receipt-ocr-fallback-guard`** — ✅ merged (PR #60). Receipt OCR silently fell back
  from the Google Vision pre-pass to the gpt-4.1 vision path (hallucinates non-Latin item names) on
  any Vision failure, with no retry/visibility/record. Fix: `visionOcrWithRetry` (1 retry on
  429/5xx/network, ~1.5s backoff; not 400/403; injectable single-attempt → testable) returning
  `{text, failReason}`; `ocrFailReason` threaded into meta; client warning chip when degraded
  (`ocr:false && failReason != 'no_key'`, `receipt.ocr_degraded_hint` ×6 native locales); best-effort
  auto-telemetry via `bug_reports` (severity `auto`, `user_id=auth.uid()`, once-per-scan, swallowed
  try/catch, NO image/item contents). Clean DeepSeek delivery, no hermetic logic miss. 14 Dart + 12
  zero-dep `node:test` cases, 508/508, analyze 0. **Investigation 1.1/1.2 (Netlify log + Vision quota)
  = controller/user infra track (needs dashboards); the new telemetry self-diagnoses future failures
  regardless of historical cause.** **Deploy `receipt-ingest` to Netlify + live Georgian-receipt
  verify pending post-merge.**
- **#4 `setall-group-list-tile-parity`** — ✅ merged (PR #61). Group expense list was the odd screen
  out: rows showed no date and the "≈ default currency" estimate was fragile. Fix (additive, no
  shared-widget refactor — deferred): `formatExpenseDate` helper (year-aware `formatShort`/
  `formatMedium`) adds a date line to both `_ExpenseTile` and `_ExpenseTileSelectable`; the ≈ estimate
  now keys off the *displayed* currency (`originalCurrency ?? currency`, not the base `currency`
  field — the L1064 mismatch bug) and, when base is USD, reads `universalUsdAmount` **directly with no
  rate-provider lookup** (removes the stale/empty `exchange_rates` dependency that silently hid it);
  non-USD keeps `rateToBaseProvider`; Decimal-only. `_ExpenseTileSelectable` promoted
  `StatelessWidget`→`ConsumerWidget` and its primary amount brought to parity (was showing base
  `currency/amount`). Both tile classes made public + `super.key` for hermetic widget tests. Removed
  the old `else if` "{currency} {amount} base" fallback (mislabeled the canonical amount; rare
  null-anchor legacy rows now show no estimate). 7 hermetic tests incl. the core guard (GEL + USD base
  + empty rates ⇒ `≈ USD 54.95`), analyze 0. Clean DeepSeek delivery — no hermetic logic miss. **Note:**
  the `concurrent_split_stress_test.dart` "< 100ms" perf assertion flakes under machine load — verified
  identical on untouched develop, not caused by this change; flagged for de-flaking. On-device verify
  (user) pending.
- **#5 `setall-group-member-net-balances`** — ✅ merged (PR #62). Members card only showed balances
  *relative to you* (third-party debtors + your own row wrongly read "settled up"). Fix: pure
  `computeMemberNetPosition` helper (Σ to − Σ from over simplified debts, Decimal; sub-cent round-first
  ⇒ "settled up") drives a primary net line for every member incl. the current user (removed the
  `m.id != myUid` gate); vs-you detail moved to a secondary line. 12 hermetic helper tests, analyze 0,
  527/527. **Controller finding:** the dispatch premise ("primary in group currency + ≈ default") was
  wrong — `simplifiedDebtsProvider` already denominates debts in the user's *default* currency, so the
  ≈ line could never fire; DeepSeek surfaced this, and the controller removed the dormant/untested ≈
  scaffolding and shipped the net-position core (resolves the user's actual ask). Group-currency
  display + ≈ (needs a base→group conversion the pipeline doesn't produce) **deferred** as an optional
  follow-up. On-device verify (user) pending.
